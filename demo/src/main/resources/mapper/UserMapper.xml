<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.demo.dao.UserMapper" >
    <resultMap id="BaseResultMap" type="com.test.demo.model.User" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="age" property="age" jdbcType="INTEGER" />
        <result column="introduction" property="introduction" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List" >
        id, name, age, introduction
    </sql>

    <select id="selectByUser" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" />
        from t_user where 1 = 1
        <if test="user.id != null">
            and id = #{user.id, jdbcType=INTEGER}
        </if>
        <if test="user.name != null">
            and name = #{user.name,jdbcType=VARCHAR}
        </if>
        <if test="user.age != null">
            and age = #{user.age,jdbcType=INTEGER}
        </if>
        <if test="user.introduction != null">
            and introduction = #{user.introduction,jdbcType=VARCHAR}
        </if>
        limit #{page.current},#{page.size}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
        delete from t_user where id = #{id,jdbcType=BIGINT}
    </delete>
    <update id="updateByUser" parameterType="com.test.demo.model.User">
        update t_user
        <set>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="age != null">
                age = #{age,jdbcType=INTEGER},
            </if>
            <if test="introduction != null">
                introduction = #{introduction,jdbcType=VARCHAR}
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <insert id="insertUsers" >
        insert into t_user(id, name, age, introduction)
        values
        <foreach collection="userList" item="user" separator=",">
            (#{user.id, jdbcType=INTEGER}, #{user.name, jdbcType=VARCHAR}, #{user.age, jdbcType=INTEGER}
            , #{user.introduction, jdbcType=VARCHAR})
        </foreach>
    </insert>
    <select id="selectUserMaxId" resultType="java.lang.Integer">
        select max(id) from t_user
    </select>
</mapper>